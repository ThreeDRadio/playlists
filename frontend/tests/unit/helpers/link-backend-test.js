import { linkBackend } from 'frontend/helpers/link-backend';
import { module, test } from 'qunit';

module('Unit | Helper | link backend');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = linkBackend([42]);
  assert.ok(result);
});
