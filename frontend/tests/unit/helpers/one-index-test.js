import { oneIndex } from 'frontend/helpers/one-index';
import { module, test } from 'qunit';

module('Unit | Helper | one index');

// Replace this with your real tests.
test('Valid integers', function(assert) {
  let result = oneIndex([42]);
  assert.ok(result);
});
