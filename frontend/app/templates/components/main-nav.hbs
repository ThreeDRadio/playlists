<nav class="navbar navbar-inverse hidden-print">
<div class="container-fluid">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    {{#link-to "playlists" class="navbar-brand"}}Three D Intranet{{/link-to}}
  </div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <ul class="nav navbar-nav">
      <li>{{#link-to "playlists.new"}}New Logging Sheet{{/link-to}}</li>
      <li>{{#link-to "playlists.today"}}Today's Plays{{/link-to}}</li>
    </ul>

    {{#if session.currentUser}}

    <ul class="nav navbar-nav navbar-right">
      <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown">
        <span class="glyphicon glyphicon-user"></span> 
        <strong>{{session.currentUser.username}}</strong>
        <span class="glyphicon glyphicon-chevron-down"></span>
      </a>
      <ul class="dropdown-menu">
        <li>
        <div class="navbar-login">
          <div class="row">
            <div class="col-lg-4">
              {{#if session.currentUser.email}}
              {{gravatar-image email=session.currentUser.email size=90}}
              {{else}}
              <p class="text-center">
              <span class="glyphicon glyphicon-user icon-size"></span>
              </p>
              {{/if}}
            </div>
            <div class="col-lg-8">
              <p class="text-left"><strong>{{session.currentUser.fullName}}</strong></p>
              <ul>
                <li>My Favourites</li>
                <li>My Playlists</li>
                <li>Settings</li>
              </ul>
            </div>
          </div>
        </div>
        </li>
        <li class="divider"></li>
        <li>
        <div class="navbar-login navbar-login-session">
          <div class="row">
            <div class="col-lg-12">
              <p>
              <button {{action 'invalidateSession'}} class="btn btn-danger btn-block">Log Out</button>
              </p>
            </div>
          </div>
        </div>
        </li>
      </ul>
      </li>
    </ul>
  {{else}}
  <ul class="nav navbar-nav navbar-right">
    <li class="divider-vertical"></li>
    <li>{{#link-to 'login'}}Login{{/link-to}}</li>
  </ul>
  {{/if}}
</div><!-- /.navbar-collapse -->
</div>


</nav>
