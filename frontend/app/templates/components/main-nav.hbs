<nav class="navbar navbar-inverse hidden-print">
 <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to "playlists" class="navbar-brand"}}Three D Intranet{{/link-to}}
    </div>

<!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
          <li>{{#link-to "playlists.new"}}New Logging Sheet{{/link-to}}</li>
          <li>{{#link-to "playlists.today"}}Today's Plays{{/link-to}}</li>
      </ul>
    <ul class="nav navbar-nav navbar-right">
        {{#if session.currentUser}}
        <li>Logged in as {{ session.currentUser.username}}</li>
        <li><a {{action 'invalidateSession'}}>Logout</a></li>
        {{else}}
        <li>{{#link-to 'login'}}Login{{/link-to}}</li>
        {{/if}}
    </ul>
    </div><!-- /.navbar-collapse -->
</div>


</nav>
